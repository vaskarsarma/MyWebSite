<!DOCTYPE html>
<html lang="en">

<head>
    <title>{{title}}</title>
    <meta charset="utf-8">
    <meta http-equiv='X-UA-Compatible' content='IE=edge' />
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">

    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

    <link rel="stylesheet" href="/css/main.css">

    <script src="/js/handlebarhelpers.js"></script>

</head>

<body>
    <nav class="navbar navbar-inverse navbar-static-top">
        <div class="container" id="navbar" class="collapse navbar-collapse">

            {{!--
            <div class="navbar-brand">Employee Registration</div>--}} {{#if user }} {{!--
            <div id="navbar6" class="navbar-collapse collapse">--}}
                <ul class="nav navbar-nav">
                    <li class="navbar-brand">Employee Registration</li>
                    <li class="active"><a href="/">Home</a></li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Employee <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="/employees/add">Add</a></li>
                            <li class="divider"></li>
                            <li><a href="/employees/list">Show</a></li>
                            {{!--
                            <li class="dropdown-header">Nav header</li>
                            <li><a href="#">Separated link</a></li>
                            <li><a href="#">One more separated link</a></li>--}}
                        </ul>
                    </li>
                    {{#IsAdmin user.admin}}
                    {{/IsAdmin}}
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="/logout">Log-out {{user.name}}</a></li>
                </ul>
                {{!--</div>--}} {{else}}
            <ul class="nav navbar-nav">
                <li class="navbar-brand">Employee Registration</li>
                <li class="active"><a href="/login">Login</a></li>
            </ul>
            {{/if}}
        </div>
    </nav>
    <div class="container">
        {{{body}}}
    </div>
</body>

</html>